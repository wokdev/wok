# CI for placeholder package
when:
  event:
    - push
  path:
    include: ["src/**", ".woodpecker.yml", "Cargo.*"]
steps:
  build:
    image: rust:1
    commands:
      - cargo build --release
      - echo "Placeholder package built successfully"
